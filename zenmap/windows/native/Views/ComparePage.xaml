<Page
    x:Class="Zenmap.Windows.Views.ComparePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="12" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ComboBox x:Name="BaselineCombo" Grid.Row="0" Header="Baseline scan" SelectionChanged="CompareSelectionChanged" />
        <ComboBox x:Name="ComparisonCombo" Grid.Row="1" Header="Comparison scan" SelectionChanged="CompareSelectionChanged" />
        <TextBox x:Name="ReportBox" Grid.Row="2" AcceptsReturn="True" IsReadOnly="True" FontFamily="Consolas" TextWrapping="Wrap" />
        <StackPanel Grid.Row="3" Orientation="Horizontal" Spacing="8">
            <Button Content="Copy Report" Click="CopyButton_Click" />
            <Button Content="Export Report" Click="ExportButton_Click" />
        </StackPanel>
    </Grid>
</Page>
