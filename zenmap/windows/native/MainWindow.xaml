<Window
    x:Class="Zenmap.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Zenmap"
    Width="1280"
    Height="820">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Padding="16,12" ColumnSpacing="12" RowSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Text="Zenmap for Windows" Style="{ThemeResource TitleTextBlockStyle}" />
            <TextBlock
                Grid.Row="1"
                Grid.Column="0"
                Grid.ColumnSpan="3"
                Text="Native WinUI 3 front end with UAC support and local saved-scan history"
                Opacity="0.75" />

            <TextBox x:Name="TargetBox" Grid.Row="2" Grid.Column="0" Width="220" Header="Target" PlaceholderText="scanme.nmap.org" TextChanged="FormFieldChanged" />
            <ComboBox x:Name="ProfileCombo" Grid.Row="2" Grid.Column="1" Header="Profile" SelectionChanged="ProfileCombo_SelectionChanged" />
            <StackPanel Grid.Row="2" Grid.Column="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Bottom">
                <Button x:Name="ScanButton" Content="Scan" Style="{ThemeResource AccentButtonStyle}" Click="ScanButton_Click" />
                <Button x:Name="StopButton" Content="Stop" Click="StopButton_Click" IsEnabled="False" />
                <Button Content="Open XML" Click="OpenXmlButton_Click" />
            </StackPanel>

            <TextBox
                x:Name="ArgumentsBox"
                Grid.Row="3"
                Grid.Column="0"
                Grid.ColumnSpan="3"
                Header="Arguments"
                TextChanged="FormFieldChanged" />
            <TextBlock
                x:Name="CommandPreviewText"
                Grid.Row="4"
                Grid.Column="0"
                Grid.ColumnSpan="3"
                FontFamily="Consolas"
                TextWrapping="WrapWholeWords"
                Opacity="0.85" />
        </Grid>

        <NavigationView
            x:Name="NavView"
            Grid.Row="1"
            PaneDisplayMode="Left"
            IsBackButtonVisible="Collapsed"
            IsSettingsVisible="True"
            SelectionChanged="NavView_SelectionChanged"
            ItemInvoked="NavView_ItemInvoked">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="Output" Tag="output" Icon="Font" />
                <NavigationViewItem Content="Hosts" Tag="hosts" Icon="Contact" />
                <NavigationViewItem Content="Ports" Tag="ports" Icon="Switch" />
                <NavigationViewItem Content="Services" Tag="services" Icon="Library" />
                <NavigationViewItem Content="Details" Tag="details" Icon="List" />
                <NavigationViewItemSeparator />
                <NavigationViewItem Content="Saved Scans" Tag="saved" Icon="Save" />
                <NavigationViewItem Content="Compare" Tag="compare" Icon="Compare" />
                <NavigationViewItem Content="Topology" Tag="topology" Icon="Map" />
                <NavigationViewItem Content="Profiles" Tag="profiles" Icon="Document" />
            </NavigationView.MenuItems>
            <Frame x:Name="ContentFrame" />
        </NavigationView>

        <Border Grid.Row="2" Padding="16,8" BorderThickness="0,1,0,0" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
            <StackPanel Spacing="4">
                <Grid>
                    <TextBlock x:Name="StatusText" Text="Ready" />
                    <TextBlock x:Name="HostCountText" HorizontalAlignment="Right" Opacity="0.75" Text="0 hosts" />
                </Grid>
                <TextBlock x:Name="ProgressText" Opacity="0.75" TextWrapping="WrapWholeWords" />
                <ProgressBar x:Name="ScanProgressBar" Maximum="100" Visibility="Collapsed" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
