<Page
    x:Class="Zenmap.Windows.Views.PortsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="12" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBox x:Name="FilterBox" Grid.Row="0" PlaceholderText="Filter ports..." TextChanged="FilterBox_TextChanged" />
        <ListView x:Name="PortsList" Grid.Row="1" DoubleTapped="PortsList_DoubleTapped">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid ColumnSpacing="12" Padding="4">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140" />
                            <ColumnDefinition Width="90" />
                            <ColumnDefinition Width="70" />
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Grid.Column="0" Text="{Binding HostAddress}" />
                        <TextBlock Grid.Column="1" Text="{Binding PortNumber}" />
                        <TextBlock Grid.Column="2" Text="{Binding State}" />
                        <TextBlock Grid.Column="3" Text="{Binding ServiceName}" />
                        <TextBlock Grid.Column="4" Text="{Binding ServiceSummary}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
